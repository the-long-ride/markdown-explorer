export const hostMessages = {
  readyAck: (overrides = {}) => ({
    command: 'readyAck' as const,
    fileList: [],
    tree: null,
    theme: 'dark',
    themeStyle: 'default',
    defaultExpanded: true,
    workspaceName: 'test-workspace',
    workspacePath: '/tmp/test-workspace',
    recentWorkspaces: [],
    appVersion: '1.5.5',
    appRuntime: 'desktop',
    hostPlatform: 'windows',
    hostArch: 'x64',
    isMaximized: false,
    documentConversionEnabled: false,
    ...overrides,
  }),
  renderContent: (overrides = {}) => ({
    command: 'renderContent' as const,
    html: '<h1>Hello</h1>',
    markdownSource: '# Hello',
    frontmatter: null,
    toc: [],
    filePath: '/tmp/test.md',
    fileName: 'test.md',
    relativePath: 'test.md',
    title: 'Hello',
    ...overrides,
  }),
  setLoading: (overrides = {}) => ({
    command: 'setLoading' as const,
    label: 'Loading...',
    detail: '',
    ...overrides,
  }),
  workspaceScanProgress: (overrides = {}) => ({
    command: 'workspaceScanProgress' as const,
    scannedFiles: 0,
    active: true,
    ...overrides,
  }),
  workspaceFilesChanged: (overrides = {}) => ({
    command: 'workspaceFilesChanged' as const,
    fileList: [],
    tree: null,
    workspaceName: 'test-workspace',
    workspacePath: '/tmp/test-workspace',
    ...overrides,
  }),
  currentFileChanged: (overrides = {}) => ({
    command: 'currentFileChanged' as const,
    filePath: '/tmp/test.md',
    ...overrides,
  }),
  recentWorkspacesChanged: (overrides = {}) => ({
    command: 'recentWorkspacesChanged' as const,
    recentWorkspaces: [],
    ...overrides,
  }),
  navNotFound: (overrides = {}) => ({
    command: 'navNotFound' as const,
    href: '/missing.md',
    ...overrides,
  }),
  workspaceUnavailable: (overrides = {}) => ({
    command: 'workspaceUnavailable' as const,
    workspacePath: '/tmp/unavailable',
    workspaceName: 'unavailable',
    reason: 'not found',
    recentWorkspaces: [],
    appVersion: '1.5.5',
    appRuntime: 'desktop',
    hostPlatform: 'windows',
    hostArch: 'x64',
    isMaximized: false,
    ...overrides,
  }),
  updateStateChanged: (overrides = {}) => ({
    command: 'updateStateChanged' as const,
    state: {
      status: 'no-update',
      version: '1.5.5',
      downloadedVersion: '',
      downloadedFileName: '',
      progressPercent: 0,
      error: '',
    },
    ...overrides,
  }),
  windowStateChanged: (overrides = {}) => ({
    command: 'window-state-changed' as const,
    isMaximized: false,
    ...overrides,
  }),
  workspaceSearchResults: (overrides = {}) => ({
    command: 'workspaceSearchResults' as const,
    requestId: 'req-1',
    results: [],
    ...overrides,
  }),
  crossTabSearchResults: (overrides = {}) => ({
    command: 'crossTabSearchResults' as const,
    requestId: 'req-1',
    results: [],
    done: true,
    total: 0,
    truncated: false,
    cancelled: false,
    error: '',
    ...overrides,
  }),
  workspaceSearchIndexLoaded: (overrides = {}) => ({
    command: 'workspaceSearchIndexLoaded' as const,
    tabs: [],
    ...overrides,
  }),
  desktopFontsResult: (overrides = {}) => ({
    command: 'desktopFontsResult' as const,
    requestId: 'fonts-1',
    fonts: [],
    ...overrides,
  }),
};

export const webviewMessages = {
  ready: (overrides = {}) => ({
    command: 'ready' as const,
    documentConversionEnabled: false,
    ...overrides,
  }),
  navigate: (path: string, overrides = {}) => ({
    command: 'navigate' as const,
    path,
    ...overrides,
  }),
  openFolder: (overrides = {}) => ({
    command: 'openFolder' as const,
    openFirstFile: true,
    ...overrides,
  }),
  openFile: (overrides = {}) => ({
    command: 'openFile' as const,
    ...overrides,
  }),
  openFileHandle: (overrides = {}) => ({
    command: 'openFileHandle' as const,
    handle: undefined,
    ...overrides,
  }),
  openPath: (path: string, overrides = {}) => ({
    command: 'openPath' as const,
    path,
    openFirstFile: true,
    ...overrides,
  }),
  openShellLocation: (
    path: string,
    mode: 'open-directory' | 'reveal-file' | 'open-parent-directory' = 'open-directory',
    overrides = {},
  ) => ({
    command: 'openShellLocation' as const,
    path,
    mode,
    ...overrides,
  }),
  activateWorkspace: (workspacePath: string, overrides = {}) => ({
    command: 'activateWorkspace' as const,
    workspacePath,
    filePath: null,
    openFirstFile: false,
    ...overrides,
  }),
  searchWorkspace: (query: string, overrides = {}) => ({
    command: 'searchWorkspace' as const,
    query,
    requestId: 'req-1',
    ...overrides,
  }),
  searchAcrossWorkspaces: (query: string, overrides = {}) => ({
    command: 'searchAcrossWorkspaces' as const,
    query,
    requestId: 'req-1',
    ...overrides,
  }),
  indexWorkspaceSearchItems: (overrides = {}) => ({
    command: 'indexWorkspaceSearchItems' as const,
    tabId: 'tab-1',
    items: [],
    ...overrides,
  }),
  loadWorkspaceSearchIndexes: (overrides = {}) => ({
    command: 'loadWorkspaceSearchIndexes' as const,
    tabId: 'tab-1',
    ...overrides,
  }),
  confirmOpenPath: (path: string, overrides = {}) => ({
    command: 'confirmOpenPath' as const,
    path,
    ...overrides,
  }),
  openRecentWorkspace: (path: string, overrides = {}) => ({
    command: 'openRecentWorkspace' as const,
    path,
    openFirstFile: false,
    ...overrides,
  }),
  deleteRecentWorkspace: (path: string, overrides = {}) => ({
    command: 'deleteRecentWorkspace' as const,
    path,
    ...overrides,
  }),
  replaceRecentWorkspaces: (recentWorkspaces: any[], overrides = {}) => ({
    command: 'replaceRecentWorkspaces' as const,
    recentWorkspaces,
    ...overrides,
  }),
  closeWorkspace: (overrides = {}) => ({
    command: 'closeWorkspace' as const,
    ...overrides,
  }),
  cancelWorkspaceScan: (workspaceOperationId = 'operation-1', overrides = {}) => ({
    command: 'cancelWorkspaceScan' as const,
    workspaceOperationId,
    ...overrides,
  }),
  cancelAllWorkspaceScans: (overrides = {}) => ({
    command: 'cancelAllWorkspaceScans' as const,
    ...overrides,
  }),
  zoomIn: (overrides = {}) => ({
    command: 'zoom-in' as const,
    ...overrides,
  }),
  zoomOut: (overrides = {}) => ({
    command: 'zoom-out' as const,
    ...overrides,
  }),
  zoomReset: (overrides = {}) => ({
    command: 'zoom-reset' as const,
    ...overrides,
  }),
  openInEditor: (path: string, overrides = {}) => ({
    command: 'openInEditor' as const,
    path,
    ...overrides,
  }),
  copyCode: (text: string, overrides = {}) => ({
    command: 'copyCode' as const,
    text,
    ...overrides,
  }),
  openExternal: (url: string, overrides = {}) => ({
    command: 'openExternal' as const,
    url,
    ...overrides,
  }),
  openHtmlPreview: (documentHtml = '<!doctype html><p>Preview</p>', overrides = {}) => ({
    command: 'openHtmlPreview' as const,
    documentHtml,
    ...overrides,
  }),
  refresh: (overrides = {}) => ({
    command: 'refresh' as const,
    ...overrides,
  }),
  setDocumentConversion: (enabled: boolean, overrides = {}) => ({
    command: 'setDocumentConversion' as const,
    enabled,
    ...overrides,
  }),
  listDesktopFonts: (overrides = {}) => ({
    command: 'listDesktopFonts' as const,
    requestId: 'fonts-list-1',
    ...overrides,
  }),
  importDesktopFonts: (overrides = {}) => ({
    command: 'importDesktopFonts' as const,
    requestId: 'fonts-import-1',
    ...overrides,
  }),
  removeImportedDesktopFont: (id = 'font_test', overrides = {}) => ({
    command: 'removeImportedDesktopFont' as const,
    requestId: 'fonts-remove-1',
    id,
    ...overrides,
  }),
  updateAppearance: (overrides = {}) => ({
    command: 'updateAppearance' as const,
    theme: 'dark',
    themeStyle: 'default',
    ...overrides,
  }),
  downloadUpdate: (overrides = {}) => ({
    command: 'downloadUpdate' as const,
    version: 'v1.5.6',
    url: 'https://example.test/update.exe',
    ...overrides,
  }),
  scheduleDownloadedUpdate: (overrides = {}) => ({
    command: 'scheduleDownloadedUpdate' as const,
    ...overrides,
  }),
  restartAndApplyUpdate: (overrides = {}) => ({
    command: 'restartAndApplyUpdate' as const,
    ...overrides,
  }),
  windowMinimize: (overrides = {}) => ({
    command: 'window-minimize' as const,
    ...overrides,
  }),
  windowMaximize: (overrides = {}) => ({
    command: 'window-maximize' as const,
    ...overrides,
  }),
  windowClose: (overrides = {}) => ({
    command: 'window-close' as const,
    ...overrides,
  }),
  toggleFullscreen: (overrides = {}) => ({
    command: 'toggle-fullscreen' as const,
    ...overrides,
  }),
};
