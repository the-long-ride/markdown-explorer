{
  "name": "markdown-explorer-workspace",
  "version": "1.5.5",
  "private": true,
  "packageManager": "pnpm@11.9.0",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:desktop": "vitest run --project desktop",
    "test:vscode": "vitest run --project vscode",
    "test:ui": "vitest run --project ui",
    "test:chromium": "vitest run --project chromium",
    "test:contracts": "vitest run --project contracts --project manifest --project build",
    "build:ui:electron": "pnpm --filter ./ui run build:electron",
    "build:ui": "pnpm --filter ./ui run build",
    "build:vscode": "pnpm --filter ./vscode run compile",
    "compile": "pnpm run build:ui && pnpm run build:vscode",
    "watch": "pnpm --filter ./vscode run watch",
    "package": "pnpm run compile && pnpm --filter ./vscode run package",
    "start:desktop": "pnpm run start:electron",
    "start:electron": "pnpm --filter ./desktop run start",
    "build:desktop": "pnpm run build:electron",
    "build:electron": "pnpm run build:ui:electron && pnpm run build:vscode && pnpm --filter ./desktop run dist",
    "build:electron:nsis": "pnpm run build:ui:electron && pnpm run build:vscode && pnpm --filter ./desktop run dist:nsis",
    "build:electron:portable": "pnpm run build:ui:electron && pnpm run build:vscode && pnpm --filter ./desktop run dist:portable",
    "build:electron:win-zip": "pnpm run build:ui:electron && pnpm run build:vscode && pnpm --filter ./desktop run dist:win-zip",
    "build:electron:mac": "pnpm run build:ui:electron && pnpm run build:vscode && pnpm --filter ./desktop run dist:mac",
    "build:electron:linux": "pnpm run build:ui:electron && pnpm run build:vscode && pnpm --filter ./desktop run dist:linux",
    "build:chromium": "pnpm --filter ./chromium-xtension run build"
  },
  "devDependencies": {
    "@testing-library/dom": "^10.4.1",
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "@testing-library/user-event": "^14.6.1",
    "@vitejs/plugin-react": "^6.0.3",
    "@vitest/coverage-istanbul": "^4.1.9",
    "@vitest/coverage-v8": "^4.1.9",
    "fake-indexeddb": "^6.2.5",
    "fast-glob": "^3.3.3",
    "jsdom": "^29.1.1",
    "vitest": "^4.1.9"
  }
}
